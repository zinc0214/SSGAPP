<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:state_checked="false" android:color="@color/textColor"/> <!-- checked -->
    <item android:state_checked="true"
        android:color="@color/textColor"/> <!-- checked -->
    <item android:color="@color/textColor"/> <!-- anything else -->
</selector>