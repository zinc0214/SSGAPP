<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.zinc0214.ssgapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".presentation.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".presentation.AddNewMemberActivity"
            android:theme="@style/MaterialTheme" />

        <activity
            android:name=".presentation.AddMoimActivity"
            android:theme="@style/MaterialTheme" />

        <activity
            android:name=".presentation.MemberManageActivity"
            android:theme="@style/MaterialTheme" />

        <activity
            android:name=".presentation.MoimManageActivity"
            android:theme="@style/MaterialTheme" />

        <activity
            android:name=".presentation.BirthdayActivity"
            android:theme="@style/MaterialTheme" />
    </application>

</manifest>